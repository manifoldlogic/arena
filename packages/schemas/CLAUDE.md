# schemas

Shared TypeScript types and validators for Arena competition data.

## Source of truth

The canonical schema definition lives in `data/schema-definition.md` (JSONL format). These TypeScript types mirror that schema for use in web-ui and tooling.

When the JSONL schema changes, update these types to match.
